{
  "_comment": "Explicitly proxy only the upstream paths we use. /api/dms-mcp is deliberately omitted so it falls through to the Edge Function at api/dms-mcp.ts. A catch-all /api/:path* would shadow the function (rewrites run before filesystem on Vercel) so we avoid one entirely.",
  "rewrites": [
    {
      "source": "/api/core-tenant/:path*",
      "destination": "https://dev.api.eveaix.com/core-tenant/:path*"
    },
    {
      "source": "/api/kcs/:path*",
      "destination": "https://dev.api.eveaix.com/kcs/:path*"
    }
  ]
}
