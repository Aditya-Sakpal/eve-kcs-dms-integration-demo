{
  "_comment": "/api/dms-mcp is handled by the Edge Function at api/dms-mcp.ts (the filesystem check runs before rewrites, so the function wins). Everything else under /api/* gets edge-rewritten straight to dev.api.eveaix.com.",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "https://dev.api.eveaix.com/:path*"
    }
  ]
}
